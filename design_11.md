# 미술학과 앱 — Design.md (v0.2)

> 참고 사이트: https://sociology-whole.vercel.app/ (Sociology-whole)
> 이 문서는 참고 사이트의 **레이아웃 틀(구조)**을 가져오되, 색상/톤은 PRD의 "모던 미니멀" 무드 + **메인 컬러: 카키(Khaki)**로 재해석한 디자인 스펙입니다.
> v0.2: **라이트 모드 / 다크 모드를 모두 지원**하도록 컬러 시스템을 토큰 기반으로 재구성했습니다. (v0.1의 오픈 이슈 1번 해결)

---

## 1. 참고 사이트 관찰 요약

참고 사이트(사회학 학습 플랫폼)를 실제로 열어 확인한 구조는 다음과 같습니다.

- **상단 고정 내비게이션**: 좌측 로고, 중앙~우측 텍스트 메뉴(용어집·이론가·시사연결·커뮤니티·자료실), 우측 끝에 필(pill) 형태의 강조 버튼(시작하기) + 아웃라인 버튼(로그인)
- **히어로 섹션**: 다크 그라데이션 배경, 중앙 정렬. 상단에 점(•)+텍스트로 된 반투명 배지 칩 → 큰 2줄 헤드라인(한 단어만 포인트 컬러로 강조) → 옅은 톤의 서브 설명 → 필 버튼 2개(채움/아웃라인) → 하단에 큰 숫자 통계 로우(구분선으로 나뉜 2~3개 지표)
- **기능 소개 섹션**: 밝은 배경으로 전환, 섹션 타이틀+부제 후 3열 카드 그리드(제목+짧은 설명)
- **강조 섹션(PRO 기능)**: "PRO 기능" 같은 라벨 배지가 붙은 카드 그리드, 프로모션성 박스(태그 리스트 + CTA)
- **콘텐츠 예시 카드**: "NEW" 배지가 붙은 카드들이 최근 콘텐츠를 미리보기 형태로 나열
- **플랜/가격 카드**: 2개 카드를 나란히 배치, 체크리스트형 기능 목록 + 하단 CTA
- **푸터**: 다단 컬럼(서비스/지원 링크) + 카피라이트 + 약관 링크

**공통 조형 언어**: 넉넉한 border-radius(카드·버튼 모두 둥글게), 필 형태 버튼, 배지 칩, 넉넉한 섹션 여백. 참고 사이트는 "다크 히어로 1개 + 이후 라이트 섹션"이 고정된 구성이었지만, 우리 앱은 **전체 페이지가 라이트/다크 두 테마를 모두 지원**하도록 확장합니다 (아래 2장).

---

## 2. 컬러 시스템 — 라이트/다크 모드 지원 (메인 컬러: 카키)

앱 전체가 **라이트 모드**와 **다크 모드**를 모두 지원합니다. 기본값은 사용자 기기(브라우저)의 시스템 설정을 따르고, 우측 상단 내비게이션에 수동 토글(해/달 아이콘)을 두어 직접 전환할 수 있게 합니다. 선택한 테마는 로그인 없이 기기(로컬 저장소)에 기억됩니다 — PRD의 "로그인 없이 기기에만 저장" 원칙과 동일한 방식.

두 모드 모두 **카키를 브랜드 컬러로 공유**하되, 명도만 서로 반전시켜 각 배경에서 가독성과 대비를 확보합니다. 아래는 역할별 토큰(변수) 정의입니다 — 실제 구현 시 CSS 변수명으로 그대로 사용 가능합니다.

### 2-1. 라이트 모드

| 토큰 | HEX | 용도 |
|---|---|---|
| `--bg` | `#FAF9F4` | 페이지 기본 배경 (화이트 대신 아주 옅은 카키 톤) |
| `--surface` | `#FFFFFF` | 카드/패널 배경 |
| `--surface-alt` | `#F5F3E8` | 섹션 구분용 옅은 배경 (Khaki 100) |
| `--ink` | `#26241A` | 제목/본문 텍스트 |
| `--muted` | `#6B6858` | 설명·캡션 텍스트 |
| `--border` | `#E4E0CE` | 구분선, 카드 보더 |
| `--brand` | `#8C8752` | 브랜드 컬러 (링크, 아이콘, 강조 보더) |
| `--brand-strong` | `#6F6A3F` | 버튼 채움 등 진한 강조 (라이트 배경에서 대비 확보) |
| `--badge-bg` | `#D8D3B0` | 배지 칩 배경 |
| `--badge-text` | `#4A4726` | 배지 칩 텍스트 |

### 2-2. 다크 모드

| 토큰 | HEX | 용도 |
|---|---|---|
| `--bg` | `#201F16` | 페이지 기본 배경 (딥 카키, 참고 사이트 히어로와 유사 톤) |
| `--surface` | `#2E2C1F` | 카드/패널 배경 |
| `--surface-alt` | `#343224` | 섹션 구분용 배경 |
| `--ink` | `#F5F3E8` | 제목/본문 텍스트 |
| `--muted` | `#B8B394` | 설명·캡션 텍스트 |
| `--border` | `#4A4830` | 구분선, 카드 보더 |
| `--brand` | `#C4BD7E` | 브랜드 컬러 (다크 배경에서 밝게, 링크·아이콘) |
| `--brand-strong` | `#A9A15A` | 버튼 채움 강조 (참고 사이트의 밝은 그린 버튼 포지션과 동일 역할) |
| `--badge-bg` | `rgba(168,161,90,0.16)` | 배지 칩 배경 (반투명) |
| `--badge-text` | `#D8D3B0` | 배지 칩 텍스트 |

> 구현 시 라이트 팔레트를 `:root` 기본값으로 두고, 다크 팔레트는 `prefers-color-scheme: dark` 미디어쿼리 + 수동 토글용 `[data-theme="dark"]` 선택자 양쪽에 동일하게 정의해 시스템 설정과 수동 토글이 둘 다 정상 동작하도록 합니다.

### 2-3. 시맨틱 컬러 (퀴즈 정답/오답 등)

| 용도 | 라이트 | 다크 |
|---|---|---|
| 정답/성공 | `#5C7A32` | `#8FAE55` |
| 오답/에러 | `#B5533B` | `#D97A63` |
| 정보/링크 강조 | `#6E7A5E` | `#9AA688` |

두 모드 모두 채도를 낮춰 카키 팔레트와 톤이 어긋나지 않도록 하고, 다크 모드에서는 배경 대비 확보를 위해 밝기를 한 단계 올렸습니다.

### 2-4. 사조(종파)별 구분 태그 컬러

카키 팔레트 안에서 명도·채도 차이로 구분(톤온톤), 라이트/다크 각각의 대비 기준에 맞춰 두 세트를 준비합니다. [TBD: 사조 목록 확정 후 매핑표 작성]

---

## 3. 타이포그래피

- 국문 본문/제목: 시스템 산세리프 우선 (예: Pretendard, "Apple SD Gothic Neo", sans-serif) — 참고 사이트와 동일하게 헤드라인은 굵게(Bold~ExtraBold), 본문은 Regular~Medium
- 헤드라인 톤: 2줄 구성, 한 단어만 `--brand-strong` 컬러로 하이라이트하는 패턴 유지 (참고 사이트의 "사회학의 **총체**를" 패턴 → 예: "미술의 **흐름**을 한눈에")
- 크기 스케일(참고): H1 40~48px / H2 28~32px / H3 20~22px / Body 15~16px / Caption 13px
- 두 모드 모두 텍스트 컬러는 `--ink`/`--muted` 토큰만 사용 — 라이트/다크 전환 시 자동으로 반전

---

## 4. 레이아웃 프레임 (섹션별 구성 및 앱 매핑)

참고 사이트의 섹션 순서를 그대로 따르되, 콘텐츠를 미술학과 앱에 맞게 치환합니다. 모든 섹션은 라이트/다크 모드에서 동일한 구조를 유지하고 색상 토큰만 전환됩니다.

| # | 참고 사이트 섹션 | 미술학과 앱으로 치환 |
|---|---|---|
| 1 | 상단 내비게이션 (용어집/이론가/시사연결/커뮤니티/자료실 + 시작하기/로그인) | 미술사 · 미술가 · 종파 · 기법 · 퀴즈 + **테마 토글(해/달 아이콘)** (로그인 버튼 없음 — 우측은 "퀴즈 풀기" 필 버튼 하나만) |
| 2 | 히어로 (배지 칩 → 헤드라인 → 서브텍스트 → CTA 2개 → 통계) | 배지: "서양미술사 편" 같은 칩 → 헤드라인: "미술의 흐름을 한눈에 경험하세요" → 서브텍스트: 화가·사조·연표를 잇는다는 설명 → CTA: "연표 보러가기"(주요) / "퀴즈 풀기"(보조) → 통계: "OO명의 화가 · OO개의 사조". 배경은 `--bg`/`--surface` 토큰 사용 — 다크 모드에서는 참고 사이트와 유사한 딥 카키 그라데이션, 라이트 모드에서는 옅은 카키 톤 배경 |
| 3 | 기능 소개 3카드 (용어집/이론가/시사연결) | 3카드: 미술가 상세 / 종파(사조) 설명 / 미술사 연표 |
| 4 | PRO 기능 카드 (AI질답/네트워킹/자료실) | 대체: 기법(유화 등) / 퀴즈 3종 / 비교·이론 — "PRO" 배지 대신 "NEW" 또는 카테고리 배지 사용 |
| 5 | NEW 콘텐츠 미리보기 카드 (시사 연결 아티클) | 최근/추천 사조·화가 카드 미리보기 (예: "인상주의는 왜 생겨났을까?") |
| 6 | AI 프로모션 박스 | 생략 또는 "가로 스크롤 타임라인 미리보기" 프로모션 박스로 대체 |
| 7 | 가격 플랜 2카드 | **제외** (로그인/결제 없는 서비스) — 대신 "학습 순서 가이드" 카드(예: 연표 → 사조 → 화가 → 퀴즈) 2~3개로 대체 가능 [TBD] |
| 8 | 푸터 (다단 링크 + 카피라이트) | 동일 구조 유지: 카테고리 링크 다단 + 출처/저작권 고지 (이미지 출처 정책 링크 포함) |

---

## 5. 컴포넌트 스타일 (토큰 기반, 라이트/다크 공용)

- **버튼**: 필(pill) 형태, radius 999px. Primary = `--brand-strong` 배경 + 배경과 대비되는 텍스트, Secondary = `--surface` 배경 + `--border` 보더
- **배지 칩**: `--badge-bg` 배경 + `--badge-text` 텍스트, radius 999px, 점(•) 아이콘 + 텍스트
- **카드**: radius 16~20px, 배경 `--surface`, 보더 `--border` 1px, 은은한 그림자(라이트: `0 2px 12px rgba(0,0,0,0.06)` / 다크: `0 2px 12px rgba(0,0,0,0.3)`)
- **통계 로우**: 큰 볼드 숫자(`--brand-strong`) + 작은 라벨(`--muted`), 세로 구분선(`--border`)
- **네비게이션**: 스크롤 시 고정(sticky), 배경 `--surface`(반투명 블러) 유지, 우측에 테마 토글 아이콘 배치
- **테마 토글**: 해/달 아이콘 스위치. 클릭 시 `[data-theme]` 속성 전환 + 로컬 저장소에 선택값 기억. 최초 진입 시 `prefers-color-scheme`을 따름

---

## 6. 반응형 규칙

- 데스크톱(≥1024px): 3열 카드 그리드, 가로 스크롤 타임라인 풀 노출
- 태블릿(768~1023px): 2열 카드 그리드
- 모바일(<768px): 1열 카드 스택, 가로 스크롤 타임라인은 그대로 유지하되 카드 폭 축소 (세로 리스트 전환 여부는 추가 결정 필요)
- 테마 토글은 모바일에서도 내비게이션 우측 고정 위치 유지

---

## 7. 오픈 이슈

1. ~~히어로를 다크로 할지 라이트로 할지~~ → **해결: 라이트/다크 모드 둘 다 지원, 시스템 설정 기본값 + 수동 토글**
2. 가격 플랜 섹션 자리에 넣을 대체 콘텐츠(학습 가이드 카드 등) 확정
3. 사조별 구분 태그 컬러의 구체적 매핑표 (사조 목록 확정 후 작업, 라이트/다크 두 세트 필요)
4. 로고/워드마크 디자인 방향 (다크 배경 위 버전과 라이트 배경 위 버전 두 가지 필요)
